DROP SCHEMA public CASCADE;
CREATE SCHEMA public;

// endpoint lists:

done:
- Accounts
- Rewards // need to implement an actual rewards system but all endpoints work

todo:
- Comments
- Levels
- Lists
- Misc
- Socials
- Songs
- Users

// misc:

- redo schemas

- fix getGJUsers20 breaking if you search for strings that only partially match usernames
  - for example, if a "lexi" account exists and u search "lex" it acts strange on both the server and client

- add proper type checking to all endpoints

- I'VE BEEN USING THE JOIN FUNCTION BUT THIS ENTIRE TIME you can just do [].join() ok so switch everything to that

- make every route use the auth plugin cuz every route has a secret anyways

- make it so i can just throw a 500 status instead of -1 every time, handle it in index.ts to return -1
  - some errors are actually a status 500 (example on uploadGJAccComment20) so idk, maybe just return -1 for those instead of copying robtop 1:1